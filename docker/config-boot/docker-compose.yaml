version: "3"

services:

   boot_server-test:
      image: boot-image
      networks:
         - my-net
      ports:
         - 7070:8080
      depends_on: 
         - db_server

   db_server-test:
      image: db-boot-image
      container_name: db_server
      restart: always
      networks:
         - my-net
      environment:
         - DB_ENVIRONMENT_VARIABLE=any_value

networks:
   my-net:

